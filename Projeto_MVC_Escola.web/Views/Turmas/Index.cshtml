@model IEnumerable<Projeto_MVC_Escola.web.Data.Entities.Turma>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>
<br />
<p class="mt-5" align="center">
    <a class="btn btn-primary" asp-action="Create">Create New</a>
</p>
<table class="table" align="center" style="width:85%">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nome_turma)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_curso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.nome_turma)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Curso.nome_curso)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
